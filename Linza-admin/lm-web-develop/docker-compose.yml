version: "3.5"

services:
  lm-ui:
    build:
      context: .
      dockerfile: dev/Dockerfile
    container_name: lm-ui
    command: sleep infinity
    volumes:
      - ./:/app/
    working_dir: /app/
    ports:
      - "3000:3000"
      - "6006:6006"
