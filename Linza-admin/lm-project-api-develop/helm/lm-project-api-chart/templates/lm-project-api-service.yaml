apiVersion: v1
kind: Service
metadata:
  name: lm-project-api-service
spec:
  ports:
    - port: 80
      targetPort: 8080
      name: http
      protocol: TCP
  selector:
    app: lm-project-api
  type: ClusterIP