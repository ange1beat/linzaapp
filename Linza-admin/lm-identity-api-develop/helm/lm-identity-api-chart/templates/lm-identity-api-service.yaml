apiVersion: v1
kind: Service
metadata:
  name: lm-identity-api-service
spec:
  ports:
    - port: 80
      targetPort: 8080
      name: http
      protocol: TCP
  selector:
    app: lm-identity-api
  type: ClusterIP